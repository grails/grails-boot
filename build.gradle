plugins {
    id 'java-library'
}

allprojects {
    group = 'org.grails'

    compileJava.options.release = 11

    repositories {
        mavenLocal()
        mavenCentral()
        maven { url "https://repo.grails.org/grails/core/" }
    }
}